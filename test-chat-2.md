@"
# Decisions
- CLI should build with tsc

# Constraints
- CLI must run offline

# Artifacts
packages/engine/src/index.ts

# Next Steps
- Finalize demo repo
"@ | Out-File -Encoding utf8 test-chat-2.md