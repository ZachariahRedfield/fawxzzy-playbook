# Project Governance

This repository uses Playbook for lightweight change governance.

## Verify rule (v0.1.0)

If code paths change (`src/**`, `app/**`, `server/**`, `supabase/**`), update `docs/PLAYBOOK_NOTES.md` in the same change.
