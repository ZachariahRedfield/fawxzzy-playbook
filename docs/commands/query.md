# `playbook query`

Query structured repository intelligence from `.playbook/repo-index.json`.

## Usage

- `playbook query architecture`
- `playbook query modules`
- `playbook query framework`
- `playbook query database`
- `playbook query rules`
- `playbook query modules --json`

## Behavior

`playbook query` reads the repository intelligence index generated by `playbook index` and returns one requested field.

The command:

1. Reads `.playbook/repo-index.json`
2. Validates `schemaVersion`
3. Returns only the requested field

Supported fields:

- `architecture`
- `framework`
- `language`
- `modules`
- `database`
- `rules`

`playbook query` never modifies repository files and never reruns repository analysis.

## Querying Repository Intelligence

```bash
playbook index
playbook query modules
playbook query architecture
playbook query modules --json
```

## JSON contract

```json
{
  "command": "query",
  "field": "modules",
  "result": ["users", "workouts"]
}
```
